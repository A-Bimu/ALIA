export default function HomePage() {
  return (
    <main>
      <h1>ALIA</h1>
      <p>
        Adaptive Learning Intelligence Agent. This deployment exposes a versioned, tenant-isolated
        API for partner learning platforms. It is a founding-pilot alpha, not a production claim.
      </p>
      <p>
        Service contract: <code>GET /api/v1/health</code>
      </p>
      <p>
        The educator console arrives in a later task. No learner data, credential, or tenant
        identifier is available from this page.
      </p>
    </main>
  );
}
